from __future__ import annotations
from typing import TYPE_CHECKING

# 只有在类型检查时才导入，运行时不导入，防止循环引用
if TYPE_CHECKING:
    from ..agent import Agent, AgentState


def templete_node(self: Agent, state: AgentState) -> AgentState:
    """模板节点"""
    pass
    return {}

def templete_branck(self: Agent, state: AgentState) -> bool:
    """模板分支"""
    return True


__all__ = ['templete_node']
